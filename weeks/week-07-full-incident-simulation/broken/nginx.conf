events {}

http {
    server {
        listen 80;

        location / {
            # BUG: No proxy timeouts — NGINX hangs waiting for dead backend
            # BUG: No error handling — returns raw 502 with no fallback
            proxy_pass http://app:8080;
        }
    }
}
